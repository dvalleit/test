name: action for testing

on:
  push:
    branches: [main]
    
  workflow_dispatch:

jobs:
  build:
    name: Dashboard 
    runs-on: ubuntu-latest
    steps: 
      - name: curl call to api
        run: |
          curl -H "Authorization: token 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" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/orgs/Blue-Express/members
