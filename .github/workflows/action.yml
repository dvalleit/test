name: action for testing

on:
  push:
    branches: [main]
    
  workflow_dispatch:

jobs:
  build:
    name: Dashboard 
    runs-on: ubuntu-latest
    steps: 
      - name: curl call to api
        run: |
          curl \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/users/bikotoru/contributions?to=2021-12-31
