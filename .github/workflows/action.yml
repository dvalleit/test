name: action for testing

on:
  push:
    branches: [main]
    
  workflow_dispatch:

jobs:
  build:
    name: Dashboard 
    runs-on: ubuntu-latest
    steps: 
      - name: curl call to api
        run: |
          curl -i -u dvalleit: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 https://api.github.com/users/octocat
